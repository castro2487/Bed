<div class="card">
    <div class="card-header clearfix">
        <h2 class="pull-left">${Resource.msg('label.profile.password','account',null)}</h2>
        <a href="${URLUtils.url('Account-EditPassword')}"  rel="nofollow" class="pull-right" aria-label="${Resource.msg('label.profile.changepassword','account',null)}">${Resource.msg('link.edit','account',null)}</a>
    </div>
    <div class="card-body">
        <dl>
            <dt class="dashboard-info headline-6">${Resource.msg('label.profile.password','account',null)}</dt>
            <dd>${pdict.account.profile.password}</dd>
        </dl>
    </div>
</div>
